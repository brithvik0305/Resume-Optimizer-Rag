GEMINI_API_KEY=your_gemini_api_key_here
GROQ_API_KEY=your_groq_api_key_here
DJANGO_SECRET_KEY=your_django_secret_key_here

# Optional: set to 1 to force Hugging Face offline mode (models must already
# be in the local cache). Leave at 0 so a fresh clone can download them.
HF_OFFLINE=0
